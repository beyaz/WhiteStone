<UserControl x:Class = "BOA.OneDesigner.WpfControls.Component"
             xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable = "d"
             x:Name = "CodeBehind"
             DataContext = "{Binding ElementName=CodeBehind}"

             d:DesignHeight = "450" d:DesignWidth = "800" >
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key = "BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <StackPanel>


        <StackPanel
            Visibility = "{Binding Info.Type.IsDivider,Mode=OneWay,Converter={StaticResource BooleanToVisibilityConverter}}"
            MinHeight = "10"
            Background = "LightGray" >
            <Label Visibility = "{Binding IsInToolbox,Mode=OneWay,Converter={StaticResource BooleanToVisibilityConverter}}" >b-divider</Label>
        </StackPanel>
    </StackPanel>
</UserControl>